# Hugo output
public/
public.tgz

# Image build artifact
image.tar.gz

# Misc.
.hugo_build.lock
.direnv/
resources/
result
public-result

# Themes are vendored via Nix, don't commit; retro lives in-repo
themes/*
!themes/retro/
